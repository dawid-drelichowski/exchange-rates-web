{{#if office}}
  <strong>{{office.name}}</strong>
  {{#with office.map as |map|}}
    {{exchange-office-map map=map}}
  {{/with}}
  <p>{{office.street}}</p>
  <p>{{office.city}}</p>
  <p>{{office.phoneNumber}}</p>
{{else}}
  {{yield}}
{{/if}}